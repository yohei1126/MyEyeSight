#ifndef DEVICE_EPORT_H
#define DEVICE_EPORT_H

/**
 * @enum eSensorPort Enum for sensor ports.
 */
typedef enum ESensorPort
{
	SENSOR_PORT_1 = 0,
	SENSOR_PORT_2,
	SENSOR_PORT_3,
	SENSOR_PORT_4
} ESensorPort;

#endif
